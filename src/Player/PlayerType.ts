// import CardType from "../CardType"

interface PlayerType {
  name: string;
  bankAmount: number;
  position: number;
  jailtime: number;
  ia: string | null;
  // cards?: Passive[];
}

export { PlayerType };
